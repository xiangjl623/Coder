# 12 文件操作

头文件：`#include <stdio.h>`

## 文件指针

```c
FILE *fp;                    /* 文件指针类型 */
fp = fopen("data.txt", "r"); /* 打开文件 */
/* ... 读写操作 ... */
fclose(fp);                  /* 关闭文件，必须调用 */
```

## fopen 打开模式

| 模式 | 含义 | 文件不存在 | 文件存在 |
|------|------|------------|----------|
| `"r"` | 只读 | 失败 | 从头读 |
| `"w"` | 只写 | 创建 | **清空**后写 |
| `"a"` | 追加 | 创建 | 末尾追加 |
| `"r+"` | 读写 | 失败 | 从头读写 |
| `"w+"` | 读写 | 创建 | 清空后读写 |
| `"a+"` | 读+追加 | 创建 | 末尾追加 |
| `"rb"` `"wb"` 等 | 二进制模式 | 同上 | 同上 |

**返回值：** 成功返回 `FILE*`，失败返回 `NULL`，务必检查。

```c
FILE *fp = fopen("test.txt", "r");
if (fp == NULL) {
    perror("打开失败");
    return 1;
}
```

## 字符级读写

| 函数 | 功能 | 返回值 |
|------|------|--------|
| `fgetc(fp)` | 读一个字符 | 字符或 EOF |
| `fputc(c, fp)` | 写一个字符 | 字符或 EOF |
| `getc(fp)` | 同 fgetc | — |
| `putc(c, fp)` | 同 fputc | — |

## 字符串/行读写

| 函数 | 功能 |
|------|------|
| `fgets(s, n, fp)` | 读一行（最多 n-1 字符） |
| `fputs(s, fp)` | 写字符串 |

## 格式化读写

| 函数 | 功能 |
|------|------|
| `fprintf(fp, fmt, ...)` | 格式化写入文件 |
| `fscanf(fp, fmt, ...)` | 格式化从文件读取 |

```c
fprintf(fp, "%d %.2f %s\n", id, score, name);
fscanf(fp, "%d %f %s", &id, &score, name);
```

## 块读写（二进制）

| 函数 | 功能 |
|------|------|
| `fread(ptr, size, count, fp)` | 读 count 个 size 字节 |
| `fwrite(ptr, size, count, fp)` | 写 count 个 size 字节 |

```c
int a[10];
fwrite(a, sizeof(int), 10, fp);
fread(a, sizeof(int), 10, fp);
```

## 文件定位

| 函数 | 功能 |
|------|------|
| `fseek(fp, offset, origin)` | 移动文件指针 |
| `ftell(fp)` | 返回当前位置 |
| `rewind(fp)` | 回到文件开头 |

**origin：** `SEEK_SET`（开头）、`SEEK_CUR`（当前）、`SEEK_END`（末尾）

## 文件状态检测

| 函数 | 功能 |
|------|------|
| `feof(fp)` | 是否到达文件末尾 |
| `ferror(fp)` | 是否发生错误 |
| `clearerr(fp)` | 清除错误和 EOF 标志 |

## 文本文件 vs 二进制文件

| | 文本模式 | 二进制模式 |
|--|----------|------------|
| 打开 | `"r"` `"w"` | `"rb"` `"wb"` |
| 内容 | 人类可读 | 原始字节 |
| 换行 | 可能转换 | 不转换 |
| 适用 | 配置文件、日志 | 结构体、图片、音频 |

## 完整示例：复制文件

```c
#include <stdio.h>
int main(void) {
    FILE *in = fopen("src.txt", "r");
    FILE *out = fopen("dst.txt", "w");
    if (!in || !out) return 1;

    int ch;
    while ((ch = fgetc(in)) != EOF)
        fputc(ch, out);

    fclose(in);
    fclose(out);
    return 0;
}
```

## 完整示例：结构体存盘

```c
typedef struct { int id; char name[20]; } Record;
Record r = {1, "Tom"};
FILE *fp = fopen("data.bin", "wb");
fwrite(&r, sizeof(Record), 1, fp);
fclose(fp);
```

## 文件操作注意事项

| 要点 | 说明 |
|------|------|
| 检查 fopen | 失败时返回 NULL |
| 必须 fclose | 否则可能丢数据、泄漏资源 |
| w 模式 | 会清空已有文件 |
| 路径 | Windows 可用 `"C:\\\\data.txt"` 或 `"C:/data.txt"` |
| 权限 | 确保有读写权限 |
